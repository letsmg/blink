services:
  app:
    build:
      context: ${RELEASE_PATH}
      dockerfile: Dockerfile
    image: ${PROJECT_NAME:-blink-hom}-app:${IMAGE_TAG:-latest}
    restart: unless-stopped
    working_dir: /var/www

    env_file:
      - ${SHARED_DIR}/.env

    environment:
      RUN_MIGRATIONS: ${RUN_MIGRATIONS:-true}

    volumes:
      - storage-data:/var/www/storage

    extra_hosts:
      - "host.docker.internal:host-gateway"

    depends_on:
      pgsql:
        condition: service_healthy
      redis:
        condition: service_healthy

  nginx:
    image: ${PROJECT_NAME:-blink-hom}-app:${IMAGE_TAG:-latest}
    restart: unless-stopped
    command: ["nginx", "-g", "daemon off;"]
    ports:
      - "${APP_PORT:-8011}:80"

    env_file:
      - ${SHARED_DIR}/.env

    volumes:      
      - storage-data:/var/www/storage

    depends_on:
      - app

    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 10s

  pgsql:
    image: postgres:16-alpine
    restart: unless-stopped
    env_file:
      - ${SHARED_DIR}/.env
    environment:
      POSTGRES_DB: ${DB_DATABASE:-blink_hom}
      POSTGRES_USER: ${DB_USERNAME:-blink_user}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-secret}
    volumes:
      - pgsql-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME:-blink_user} -d ${DB_DATABASE:-blink_hom}"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: redis-server --requirepass ${REDIS_PASSWORD:-secret}
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD:-secret}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

networks:
  default:
    name: ${DOCKER_NETWORK:-blink-hom-net}
    external: true

volumes:
  storage-data:
    external: true
    name: ${PROJECT_NAME:-blink-hom}-storage-data
  pgsql-data:
    external: true
    name: ${PROJECT_NAME:-blink-hom}-pgsql-data
  redis-data:
    external: true
    name: ${PROJECT_NAME:-blink-hom}-redis-data